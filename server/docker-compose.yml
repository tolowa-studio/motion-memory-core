# Motion Memory Core — self-host stack.
#   postgres  → the memory store (pgvector)
#   stash     → the memory engine (built from source; Apache-2.0, see NOTICE)
#   gateway   → bearer-auth reverse proxy; the only service you expose
#
# Only `gateway` publishes a port. Point your MCP clients at http://<host>:8080/mcp
# with `Authorization: Bearer $STASH_TOKEN`.
services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: stash
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: stash
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stash"]
      interval: 10s
      timeout: 5s
      retries: 10

  stash:
    build:
      context: .
      dockerfile: deploy/Dockerfile.stash
    environment:
      # Stash's own config loader requires ALL of these — there are no
      # built-in defaults, confirmed by actually booting this stack (a
      # partial env list fails fast with "required environment variable ...
      # is not set"). Values below match the live, working production deploy.
      STASH_POSTGRES_DSN: postgres://stash:${POSTGRES_PASSWORD}@postgres:5432/stash?sslmode=disable
      STASH_OPENAI_API_KEY: ${STASH_OPENAI_API_KEY}
      STASH_OPENAI_BASE_URL: ${STASH_OPENAI_BASE_URL:-https://api.openai.com/v1}
      STASH_EMBEDDING_MODEL: ${STASH_EMBEDDING_MODEL:-openai/text-embedding-3-small}
      STASH_REASONER_MODEL: ${STASH_REASONER_MODEL:-openrouter/free}
      STASH_VECTOR_DIM: ${STASH_VECTOR_DIM:-1536}
      STASH_MAX_RESULT_SIZE: ${STASH_MAX_RESULT_SIZE:-10000}
      STASH_CONTEXT_TTL: ${STASH_CONTEXT_TTL:-1h}
      STASH_HTTP_ADDR: :8080
      STASH_LOG_LEVEL: ${STASH_LOG_LEVEL:-info}
      STASH_LOG_FORMAT: ${STASH_LOG_FORMAT:-json}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  gateway:
    build:
      context: ./gateway
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      STASH_TOKEN: ${STASH_TOKEN}
      STASH_UPSTREAM: stash:8080
    depends_on:
      - stash
    restart: unless-stopped

  # Optional: scheduled Postgres backup to S3-compatible storage (R2, S3, etc.).
  # Not started by default. Set AWS_*/S3_* in .env, then:
  #   docker compose --profile backup run --rm backup
  # See backup/README.md for restore instructions.
  backup:
    build:
      context: ./backup
    profiles: ["backup"]
    environment:
      DATABASE_URL: postgres://stash:${POSTGRES_PASSWORD}@postgres:5432/stash?sslmode=disable
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      S3_BUCKET: ${S3_BUCKET:-}
      S3_ENDPOINT_URL: ${S3_ENDPOINT_URL:-}
      S3_PREFIX: ${S3_PREFIX:-postgres}
      RETENTION_DAYS: ${RETENTION_DAYS:-30}
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
